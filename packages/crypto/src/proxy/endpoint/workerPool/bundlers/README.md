Worker bundling syntax and optimization options are bundler-specific.
For this reason, this folder exports a number of custom providers.
It is recommended to pick the provider corresponding to the bundler being used, if available.
Otherwise, `genericProvider.ts` should be compatible with modern tools.
